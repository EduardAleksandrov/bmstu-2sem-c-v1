#include <stdio.h>




int main(void)
{
    // char ch;
    // ch = getchar();
    // putchar(ch);

    printf("%x %#x", 10, 10);

    int i, j;
    scanf("%d%d", &i, &j);
    printf("%d %d", i, j);

    return 0;
}